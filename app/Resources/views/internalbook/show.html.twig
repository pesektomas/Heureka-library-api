{% extends 'base.html.twig' %}

{% block body %}
    <h1>InternalBook</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ internalBook.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{% if internalBook.date %}{{ internalBook.date|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Book</th>
                <td>{{ internalBook.book }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('internalbook_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('internalbook_edit', { 'id': internalBook.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
